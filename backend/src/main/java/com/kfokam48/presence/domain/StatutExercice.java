package com.kfokam48.presence.domain;

public enum StatutExercice {
    DEPOSE,
    EN_ATTENTE,
    /** Étape 3 : un seul des deux relecteurs a rendu sa relecture (RG17). */
    PROVISOIRE,
    RELU
}
